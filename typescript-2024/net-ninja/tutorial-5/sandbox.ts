// explicit types
let character: string;

// arrays
let ninjas: string[];

// need to initialize empty array to push eg. ...string[] = [];

// union type (1 of 2 or 1 of 3 types)
// pipe, or
let mixed: (string | number)[] = [];
let uid: string | number;

// objects
let ninjaOne: object;

let ninjaTwo: {
  name: string,
  age: number,
  beltColor: string
};
