// arrays
let names = ['luigi', 'mario', 'yoshi'];
let numbers = [10, 20, 30, 40];
let mixed = ['yo', 1, true];

// objects
let ninja = {
  name: 'mario',
  belt: 'black',
  age: 30
};

// can't add more properties
// can override but need same properties
